import React from "react";
import { Route, HashRouter, Switch } from "react-router-dom";

const Routers = () => {
  return (
    <HashRouter>
      <Switch></Switch>
    </HashRouter>
  );
};

export default Routers;
